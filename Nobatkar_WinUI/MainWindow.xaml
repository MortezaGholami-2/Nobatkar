<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Nobatkar_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nobatkar_WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Nobatkar_WinUI">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Height="48" Background="#FF2D2D30" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Padding="12,0,0,0">
                <TextBlock Text="🌓 نوبت‌کار" FontSize="18" FontWeight="Bold" Foreground="White" />
            </StackPanel>

            <!--<StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                <Button Style="{StaticResource AppWindowTitleBarMinimizeButtonStyle}" />
                <Button Style="{StaticResource AppWindowTitleBarMaximizeButtonStyle}" />
                <Button Style="{StaticResource AppWindowTitleBarCloseButtonStyle}" />
            </StackPanel>-->
        </Grid>

        <!-- Page Content -->
        <Frame x:Name="RootFrame" Margin="0,48,0,0" />
    </Grid>
</Window>
